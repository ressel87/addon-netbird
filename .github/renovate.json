{
  "$schema": "https://docs.renovatebot.com/renovate-schema.json",
  "rebaseWhen": "behind-base-branch",
  "gitIgnoredAuthors": [
    "github-actions[bot]@users.noreply.github.com"
  ],
  "dependencyDashboard": true,
  "labels": [
    "dependencies",
    "no-stale"
  ],
  "commitMessagePrefix": "⬆️",
  "commitMessageTopic": "{{depName}}",
  "customManagers": [
    {
      "customType": "regex",
      "fileMatch": [
        "/Dockerfile$",
        "/build.yaml$"
      ],
      "matchStringsStrategy": "any",
      "matchStrings": [
        "ARG BUILD_FROM=(?<depName>.*?):(?<currentValue>.*?)\\s+",
        "(aarch64|amd64):\\s[\"']?(?<depName>.*?):(?<currentValue>.*?)[\"']?\\s"
      ],
      "datasourceTemplate": "docker"
    },
    {
      "customType": "regex",
      "fileMatch": [
        "/Dockerfile$"
      ],
      "matchStringsStrategy": "any",
      "matchStrings": [
        "\\s\\s(?<package>[a-z0-9][a-z0-9-_]+)=(?<currentValue>[a-z0-9-_.]+)\\s+"
      ],
      "versioningTemplate": "loose",
      "datasourceTemplate": "repology",
      "depNameTemplate": "alpine_3_24/{{package}}",
      "description": "apk pins in Dockerfile. The alpine_3_xx repo must match the Alpine version of ghcr.io/hassio-addons/base (see hassio-addons/addon-base base/build.yaml); update it when the base image moves to a new Alpine release."
    },
    {
      "customType": "regex",
      "fileMatch": [
        "/Dockerfile$"
      ],
      "matchStrings": [
        "netbirdio/netbird:[\"']?(?<currentValue>.+?)[\"']?\\s+"
      ],
      "datasourceTemplate": "github-releases",
      "versioningTemplate": "loose",
      "depNameTemplate": "netbirdio/netbird",
      "extractVersionTemplate": "^v?(?<version>.+)$"
    },
    {
      "customType": "regex",
      "fileMatch": [
        "/config.yaml$"
      ],
      "matchStrings": [
        "version:\\s+[\"']?(?<currentValue>.+?)[\"']?\\s+"
      ],
      "datasourceTemplate": "github-releases",
      "versioningTemplate": "loose",
      "depNameTemplate": "netbirdio/netbird",
      "extractVersionTemplate": "^v?(?<version>.+)$"
    }
  ],
  "packageRules": [
    {
      "matchDatasources": [
        "repology"
      ],
      "automerge": true
    },
    {
      "description": "TEMPORARY: repology.org is offline (domain on registrar hold since 2026-09-13, see https://github.com/repology/repology-rs/issues/560). Remove this rule when repology.org resolves again.",
      "matchDatasources": [
        "repology"
      ],
      "enabled": false
    },
    {
      "matchDatasources": [
        "docker"
      ],
      "matchPackageNames": [
        "netbirdio/netbird"
      ],
      "enabled": false
    },
    {
      "groupName": "Add-on base image",
      "matchDatasources": [
        "docker"
      ],
      "matchUpdateTypes": [
        "minor",
        "patch"
      ],
      "automerge": true
    },
    {
      "description": "NetBird: group updates, add label for changelog automation, automerge minor/patch only",
      "groupName": "NetBird",
      "matchDatasources": [
        "github-releases"
      ],
      "matchDepNames": [
        "netbirdio/netbird"
      ],
      "addLabels": [
        "netbird-update"
      ]
    },
    {
      "description": "NetBird: automerge minor/patch (major updates require manual review)",
      "matchDatasources": [
        "github-releases"
      ],
      "matchDepNames": [
        "netbirdio/netbird"
      ],
      "matchUpdateTypes": [
        "minor",
        "patch"
      ],
      "automerge": true
    }
  ]
}
